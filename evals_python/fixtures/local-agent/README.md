# Local Agent Eval Cases

Append failed local Agent Mode runs to `local_agent_cases.csv`. Do not run these cases automatically yet; this CSV is the seed set for the upcoming local Gemma E2B working-agent regression lane.

